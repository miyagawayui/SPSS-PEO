{
  "name": "project",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "@sap/approuter": "^14.4.2",
    "@sap/html5-repo-mock": "^2.1.5"
  },
  "scripts": {
    "build-zpoux001": "cd zpoux001 && if [ ! -d ${workspaceFolder}/ZPOUX001/zpoux001/node_modules/react-scripts ] && [ ! -d ${workspaceFolder}/ZPOUX001/node_modules/react-scripts ]; then npm i; fi && npm run build"
  }
}
