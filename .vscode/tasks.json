{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run zpoux001",
      "type": "shell",
      "dependsOn": "Build zpoux001",
      "command": "if [ ! -f ${workspaceFolder}/node_modules/@sap/html5-repo-mock/index.js ] || [ ! -f ${workspaceFolder}/node_modules/@sap/approuter/approuter.js ]; then npm i @sap/approuter && npm i @sap/html5-repo-mock; fi",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    },
    {
      "label": "Build zpoux001",
      "type": "shell",
      "command": "if [ ! -d ${workspaceFolder}/zpoux001/build ]; then npm run build-zpoux001; fi",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    },
    {
      "label": "Run topic-ZPOUX001-fj1",
      "type": "shell",
      "dependsOn": "Build topic-ZPOUX001-fj1",
      "command": "if [ ! -f ${workspaceFolder}/node_modules/@sap/html5-repo-mock/index.js ] || [ ! -f ${workspaceFolder}/node_modules/@sap/approuter/approuter.js ]; then npm i @sap/approuter && npm i @sap/html5-repo-mock; fi",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    },
    {
      "label": "Build topic-ZPOUX001-fj1",
      "type": "shell",
      "command": "if [ ! -d ${workspaceFolder}/zpoux001/build ]; then npm run build-zpoux001; fi",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}